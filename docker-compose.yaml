services:

#   kafka:
#     image: confluentinc/cp-kafka
#     ports:
#     - 9092:9092
#     environment:
#       KAFKA_KRAFT_MODE: 'true'
#       KAFKA_PROCESS_ROLES: CONTROLLER

#       KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT, PLAINTEXT:PLAINTEXT, PLAINTEXT_HOST:PLAINTEXT
#       KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka-kraft:9092, PLAINTEXT_HOST://localhost:9092
#       KAFKA_LISTENERS: PLAINTEXT://kafka-kraft:9092, CONTROLLER://kafka-kraft:9093, PLAINTEXT_HOST://127.0.0.1:9092

#       #advertised.listeners: INTERNAL://kafka:9092, EXTERNAL://localhost:9092
#       KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT


# # -e KAFKA_CONTROLLER_LISTENER_NAMES='CONTROLLER' \
# # -e CLUSTER_ID='MkU3OEVBNTcwNTJENDM2Qk' \

  kafka:
    image: confluentinc/cp-kafka:7.8.3
    container_name: kafka
    ports:
      - "9092:9092"

    environment:
      KAFKA_KRAFT_MODE: 'true'

      CLUSTER_ID: '1L6g7nGhU-eAKfL--X25wo'
      KAFKA_NODE_ID: 1

      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER

      KAFKA_LOG_DIRS: /tmp/kraft-combined-logs

    volumes:
      - kafka_kraft:/var/lib/kafka/data

  ingestion:
    build: ingestion
    environment:
      KAFKA_HOST: kafka
      KAFKA_PORT: 9092

volumes:
  kafka_kraft: